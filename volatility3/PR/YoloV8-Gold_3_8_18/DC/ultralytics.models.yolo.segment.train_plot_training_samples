plot_images((batch['img']), (batch['batch_idx']),
  (batch['cls'].squeeze(-1)),
  (batch['bboxes']),
  (batch['masks']),
  paths=(batch['im_file']),
  fname=(self.save_dir / f"train_batch{ni}.jpg"),
  on_plot=(self.on_plot))