tl = target.long()
tr = tl + 1
wl = tr - target
wr = 1 - wl
return (F.cross_entropy(pred_dist, (tl.view(-1)), reduction='none').view(tl.shape) * wl + F.cross_entropy(pred_dist, (tr.view(-1)), reduction='none').view(tl.shape) * wr).mean((-1),
  keepdim=True)