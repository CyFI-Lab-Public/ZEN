if isinstance(boxes, torch.Tensor):
    boxes[(Ellipsis, 0)] = boxes[(Ellipsis, 0)].clamp(0, shape[1])
    boxes[(Ellipsis, 1)] = boxes[(Ellipsis, 1)].clamp(0, shape[0])
    boxes[(Ellipsis, 2)] = boxes[(Ellipsis, 2)].clamp(0, shape[1])
    boxes[(Ellipsis, 3)] = boxes[(Ellipsis, 3)].clamp(0, shape[0])
else:
    boxes[(..., [0, 2])] = boxes[(..., [0, 2])].clip(0, shape[1])
    boxes[(..., [1, 3])] = boxes[(..., [1, 3])].clip(0, shape[0])
return boxes