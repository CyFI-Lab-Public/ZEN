if not (isinstance(self.imgsz, (tuple, list)) and self.imgsz[0] == self.imgsz[1]):
    raise AssertionError(f"SAM models only support square image size, but got {self.imgsz}.")
self.model.set_imgsz(self.imgsz)
return return self.model.image_encoder(im)