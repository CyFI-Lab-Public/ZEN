from PIL import Image
if x.is_cuda:
    x = x.cpu().numpy().squeeze()
elif x.shape[-1] != 3:
    if len(x.shape) == 4:
        x = (np.moveaxis(x, 1, -1) * 255).astype(np.uint8)[0].squeeze()
    else:
        if len(x.shape) == 3:
            x = (np.moveaxis(x, 0, -1) * 255).astype(np.uint8)
img = Image.fromarray(x)
img.show()