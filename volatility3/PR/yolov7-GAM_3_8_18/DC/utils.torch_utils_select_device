s = f"Y\x00\x00\x00O\x00\x00\x00L\x00\x00\x00O\x00\x00\x00R\x00\x00\x00 \x00\x00\x00\x80ö\x01\x00 \x00\x00\x00{git_describe() or date_modified()} torch {torch.__version__} "
cpu = device.lower() == 'cpu'
if cpu:
    os.environ['CUDA_VISIBLE_DEVICES'] = '-1'
else:
    if device:
        os.environ['CUDA_VISIBLE_DEVICES'] = device
    else:
        cuda = not cpu and torch.cuda.is_available()
        if cuda:
            n = torch.cuda.device_count()
            if n > 1:
                if batch_size:
                    assert batch_size % n == 0, f"batch-size {batch_size} not multiple of GPU count {n}"
            space = ' ' * len(s)
            for i, d in enumerate(device.split(',') if device else range(n)):
                p = torch.cuda.get_device_properties(i)
                s += f"{'' if i == 0 else space}CUDA:{d} ({p.name}, {p.total_memory / 1048576}MB)\n"

        else:
            s += 'CPU\n'
    logger.info(s.encode().decode('ascii', 'ignore') if platform.system() == 'Windows' else s)
    return     return torch.device('cuda:0' if cuda else 'cpu')