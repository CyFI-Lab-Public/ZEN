for m in model.modules():
    t = type(m)
    if t is nn.Conv2d:
        pass
    elif t is nn.BatchNorm2d:
        m.eps = 0.001
        m.momentum = 0.03
    elif t in (nn.Hardswish, nn.LeakyReLU, nn.ReLU, nn.ReLU6):
        m.inplace = True